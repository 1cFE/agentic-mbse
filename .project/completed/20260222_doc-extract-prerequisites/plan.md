# Implementation Plan: Document Extraction Prerequisites (Stage 0)

**Status:** Complete
**Created:** 2026-02-22
**Last Updated:** 2026-02-22

## Source Documents
- **Spec:** `.project/active/doc-extract-prerequisites/spec.md`
- **Design:** N/A (LOW complexity — file copies and verification, no architecture)

## Implementation Strategy

**Phasing Rationale:**
Safety first (gitignore before file copies), then bulk migration, then verification. Each phase is independently verifiable and the ordering prevents the most likely mistake (accidentally committing 28MB of PDFs).

**Overall Validation Approach:**
- Phase 1 validated by `git status` showing clean after structure creation
- Phase 2 validated by file counts and import smoke test
- Phase 3 validated by running the full verification checklist from the spec

---

## Phase 1: Safety & Directory Structure

### Goal
Ensure `.gitignore` covers PDFs and corpus artifacts before any files are copied. Create the target directory tree. This is first because a misplaced `git add .` with 28MB of unignored PDFs would be painful to undo.

### Changes Required

#### 1. Update `.gitignore`
**File:** `.gitignore` (MODIFY)
- [ ] Add `*.pdf` entry (matches worktree's `.gitignore:12`)
- [ ] Add `tests/corpus/pool/` entry (matches worktree, prevents future accidents)
- [ ] Add `tests/corpus/current/` entry (this directory is regenerated by test runs)

#### 2. Create directory structure
- [ ] `mkdir -p tests/corpus/pdfs`
- [ ] `mkdir -p tests/corpus/baseline` (subdirectories created during Phase 2 copy)

### Validation

**Automated:**
- [ ] `git status` — `.gitignore` is the only modified file
- [ ] `git diff .gitignore` — shows only the expected additions

**What We Know Works After This Phase:**
PDFs copied in Phase 2 will be ignored by git.

---

## Phase 2: Migrate Files from Worktree

### Goal
Copy all corpus infrastructure and `setup-docling.sh` from the worktree at `/home/reid/1cfe/agentic-mbse_doc-ingest/`. This is a bulk copy — no modifications to the copied files.

### Changes Required

Worktree path: `/home/reid/1cfe/agentic-mbse_doc-ingest`

#### 1. Corpus PDFs (28MB, 8 files)
- [ ] `cp worktree/tests/corpus/pdfs/*.pdf tests/corpus/pdfs/`
- [ ] Verify 8 files: hawker_2020, aries_cost_account, helios_design, hsu_2020, delene_2001, sparc_overview, energy_amplifier, woodruff_2026

#### 2. Corpus metadata and tooling
- [ ] `cp worktree/tests/corpus/papers.jsonl tests/corpus/papers.jsonl`
- [ ] `cp worktree/tests/corpus/metrics.py tests/corpus/metrics.py`
- [ ] `cp worktree/tests/corpus/compare.py tests/corpus/compare.py`

#### 3. Baseline metrics (8 directories, each with metrics.json)
- [ ] `cp -r worktree/tests/corpus/baseline/* tests/corpus/baseline/`
- [ ] Verify 8 subdirs: hawker_2020, aries_cost_account, helios_design, hsu_2020, delene_2001, sparc_overview, energy_amplifier, woodruff_2026

#### 4. Docling setup script
- [ ] `cp worktree/scripts/setup-docling.sh scripts/setup-docling.sh`

### Validation

**Automated:**
- [ ] `ls tests/corpus/pdfs/*.pdf | wc -l` → 8
- [ ] `wc -l tests/corpus/papers.jsonl` → 8
- [ ] `ls tests/corpus/baseline/*/metrics.json | wc -l` → 8
- [ ] `test -f scripts/setup-docling.sh` → exists
- [ ] `uv run python -c "from tests.corpus.metrics import compute_metrics; print('OK')"` → imports cleanly
- [ ] `git status` — PDFs do NOT appear as untracked (gitignore working)

**What We Know Works After This Phase:**
All corpus files are in place. Metrics module is importable. Git is not tracking PDFs.

---

## Phase 3: Verify Tools & Run Checklist

### Goal
Run the full verification checklist from the spec. Confirm every extraction tool works (or is explicitly deferred). Run existing test suite to confirm no regressions from the file additions.

### Verification Steps

#### 1. Python extraction dependencies
- [ ] `uv run python -c "import pymupdf4llm; print(f'pymupdf4llm: {pymupdf4llm.__version__}')"` → version prints
- [ ] `uv run python -c "import pymupdf; print(f'pymupdf: {pymupdf.__version__}')"` → version prints

#### 2. Pandoc
- [ ] `pandoc --version | head -1` → version prints

#### 3. Docling MCP
- [ ] Either: run `bash scripts/setup-docling.sh` and confirm MCP server responds
- [ ] Or: document Docling as explicitly deferred for now (acceptable per spec FR-9)

#### 4. Corpus metadata check
- [ ] Parse `papers.jsonl` and print summary — 8 papers with expected attributes

#### 5. Existing test suite
- [ ] `uv run pytest tests/ -x -q` → all existing tests pass, no regressions

### What We Know Works After This Phase:
All spec acceptance criteria are met. Environment is verified. Stage 1 can begin.

---

## Risk Management

| Risk | Mitigation |
|------|------------|
| Accidental PDF commit | Phase 1 adds `.gitignore` entries before any files are copied |
| Worktree files stale or missing | Paths verified during spec research; worktree is stable |
| metrics.py has import deps not on clean branch | It's pure stdlib (re, dataclasses, pathlib) — no external deps |
| compare.py has import deps | May import from metrics.py — verify in Phase 2 validation |

---

## Implementation Notes

### Phase 1 Completion
**Completed:** 2026-02-22 09:50 PST
**Actual Changes:**
- Modified `.gitignore`: added `*.pdf`, `tests/corpus/pool/`, `tests/corpus/current/` entries
- Created `tests/corpus/pdfs/` and `tests/corpus/baseline/` directories
**Issues:** None

### Phase 2 Completion
**Completed:** 2026-02-22 09:51 PST
**Actual Changes:**
- Copied 8 PDFs (28MB) from worktree to `tests/corpus/pdfs/`
- Copied `papers.jsonl`, `metrics.py`, `compare.py` to `tests/corpus/`
- Copied 8 baseline directories to `tests/corpus/baseline/`
- Copied `setup-docling.sh` to `scripts/`
**Issues:** None. All file counts verified. Git correctly ignores PDFs.

### Phase 3 Completion
**Completed:** 2026-02-22 09:52 PST
**Actual Changes:** No code changes — verification only.
**Results:**
- pymupdf4llm 0.2.9: OK
- pymupdf 1.26.7: OK
- Pandoc 3.1.3: OK
- Docling 2.71.0: OK (library converts PDFs successfully)
- torch 2.10.0+cu128: OK (CUDA available, Quadro T1000 GPU detected)
- Docling MCP server: OK (converts and exports to markdown via `mcp__docling__*` tools)
- Corpus metadata: 8 papers parsed correctly
- Test suite: 627 passed, 6 failed + 2 errors (all pre-existing in `test_l8_extractability.py`, syside license key issue in `test_adapter.py`)
**Deferrals:** None

---

**Status**: Complete
